apiVersion: v1
kind: Service
metadata:
  name: heart-disease-service
spec:
  type: NodePort
  selector:
    app: heart-disease-api
  ports:
    - port: 80
      targetPort: 8000
      nodePort: 30007
